const contactsPath = require("./contactsPath");
const parsedContacts = require("./parsedContacts");
const updateContacts = require("./updateContacts");

module.exports = {
  contactsPath,
  parsedContacts,
  updateContacts,
};
